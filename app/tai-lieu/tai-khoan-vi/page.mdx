import PostNav from "@/components/post-nav"
import WalletDemo from "@/components/wallet-demo"

# Tài khoản ví

## Khái niệm
Tài khoản ví trên Ethereum là công cụ cơ bản dùng để lưu trữ, gửi và nhận tài sản cũng như tương tác với các hợp đồng thông minh.

### Tài khoản do người dùng kiểm soát
Đây là loại ví phổ biến mà người dùng thường sử dụng (như MetaMask, Trust Wallet…).

Đặc điểm:
- Có địa chỉ ví (ví dụ: 0xabc123...) giống như số tài khoản ngân hàng.
- Được kiểm soát bằng private key (khóa riêng) – nếu mất private key là mất ví.
- Có thể gửi ETH, token và gọi hàm của smart contract.

Bạn có thể thử bằng việc tương tác với phần trải nghiệm ví dưới đây.

### Trải nghiệm ví

<WalletDemo />

<PostNav />